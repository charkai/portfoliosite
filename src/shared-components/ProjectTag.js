const ProjectTag = ({tag}) => {
    return (
        <span class="inline-flex bg-purple-100 text-purple-800 text-sm font-medium px-2.5 py-0.5 rounded w-max">{tag}</span>
    );
}

export default ProjectTag;